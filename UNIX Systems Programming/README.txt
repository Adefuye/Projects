The secretfinder.sh file is used to find which file has the "SECRET AUTH CODE"
by extracting files from the grep.tar file and searhing throgh all of them.
To run the script type the command:

bash secretfinder
